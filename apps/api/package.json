{
  "name": "@reva/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --port 8000",
    "dev:worker": "uv run celery -A app.workers.celery_app worker --loglevel=info",
    "start": "uv run uvicorn app.main:app --port 8000",
    "format": "uv run ruff format ."
  }
}
